- [ ] Run bot review — tick to kick off the Claude review. It un-ticks itself when the run starts; tick again for another round.

## Summary

<!-- bullet points — what was built/changed -->

## Test plan

- [ ] `cargo build` passes with no warnings
- [ ] `cargo test` passes — <N> new tests:
  - `test_name` — <one sentence: what it verifies>
- [ ] Manual smoke test (if the change has a visible surface)

Closes #<issue-number>
